namespace CameraOperator
{
    // Names the viewpoints that a host director can request around the target.
    public enum CameraPlacement
    {
        Front,
        Left,
        Right,
        FrontTop,
        LeftTop,
        RightTop
    }
}
